import frontend.{IR, SetVariable, StringType}
import org.scalatest.funsuite.AnyFunSuite

class L2 extends AnyFunSuite {
  test("l2") {
    val ir = IR
    val external_ingress_ports = SetVariable("ei", StringType)
  }
}
